version: "2"

linters:
  default: standard
  enable:
    - gocritic

linters-settings:
  gocritic:
    enabled-tags:
      - diagnostic
      - experimental
      - opinionated
      - performance
      - style
